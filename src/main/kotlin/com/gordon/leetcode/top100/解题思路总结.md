## 字母异位词分组
所谓的字母异位词,就是如果两个字符串中的所有字符是一样的,不需要顺序,它们就是字母异位词,比如
bat和abt就是满足的

将一个List<String>按照字母异位词分组,就需要一个Map<Key,List<String>>,这里的key就是所有的
字母异位词都可以转换的,并且是一致的.

首先String-->CharArray-->排序-->String. 最终的结果就可以当做key.
```kotlin
str.toCharArray().apply { sort() }.contentToString()
```

